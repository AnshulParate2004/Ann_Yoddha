Ann Yoddha Context And Next Steps
Last updated: 2026-04-08
Workspace root: C:\Users\aryan\Desktop\Subjects\Coding\Ann_Yoddha

Goal for today
- Complete the app to a stable demo-ready and deployment-ready state.
- Do not treat this as a loose backlog. Treat this as a finish-order execution document.
- Primary focus today:
  - stabilize backend runtime
  - finish missing analytics/analysis requirements
  - redesign ann-yoddha-mobile UI/UX to look professional and easier to use
  - verify web + mobile + backend flows
  - prepare deployment checklist and environment variables

Project structure
- Mobile app: ann-yoddha-mobile
- Web app: ann-yoddha-harvest-help
- Backend API: ann_yoddha_backend
- Root handoff file: context.txt

Current implementation status

Backend already implemented
- Real FastAPI app exists at ann_yoddha_backend/app/main.py
- Auth utilities exist at ann_yoddha_backend/app/auth.py
- Database models exist at ann_yoddha_backend/app/db/models.py
- DB session bootstrap exists at ann_yoddha_backend/app/db/session.py
- Inference helper exists at ann_yoddha_backend/app/services/inference.py
- Compatibility shim exists at ann_yoddha_backend/main.py
- JWT auth is wired with login/register/me
- Protected POST /predict exists
- Protected POST /sync exists
- SQLite local DB is enabled by default
- Prediction saves history records
- Offline sync endpoint bulk inserts scan history rows

Backend behavior currently expected
- POST /auth/register
- POST /auth/login
- GET /auth/me
- GET /api/v1/auth/me
- GET /health
- POST /predict
- POST /sync
- Auth-protected routes require Bearer token
- Model file used: ann_yoddha_backend/models/wheat_model.keras

Mobile already implemented
- AuthProvider exists and restores stored token
- Login and register screens exist
- Root auth gating exists
- Bottom-tab navigation exists
- Scanner uploads to protected backend predict endpoint
- Local SQLite save exists
- Offline scans can be marked unsynced and later synced
- Dashboard and history screens already show basic diagnosis information

Web already implemented
- Web auth context uses backend login/register/me
- Protected diagnosis flow exists
- Dashboard reads backend history
- Analytics page exists but is still demo-mode and depends on placeholder backend data

Reality check: what is not done yet
- Full runtime verification is not complete
- Backend dependency/runtime verification is still a real risk
- Analytics/analysis is not production-ready
- Mobile UI/UX is improved from baseline but is not yet fully professional or tightly designed
- Deployment config is not finalized
- End-to-end testing across backend + web + mobile is still incomplete

What needs to be completed today

Priority 1: backend must be stable enough to support web and mobile
- Confirm backend installs and starts cleanly in the current environment
- Confirm TensorFlow/Keras model loads without startup/import issues
- Confirm login/register/me work from real requests
- Confirm /predict works with real image upload and protected auth
- Confirm /sync works with real offline scan payloads
- Add or fix any missing response validation, error handling, or CORS issues
- Confirm history endpoint behavior used by web dashboard

Priority 2: analysis section must move from placeholder to defined product scope
- Define exactly what the analysis section shows in version 1
- If true analytics backend cannot be completed today, ship a reduced but honest version instead of fake intelligence
- Replace vague placeholder wording with user-meaningful insights derived from real stored scan history
- Decide whether the analysis page will show:
  - scan totals
  - healthy vs diseased ratio
  - disease frequency
  - recent trend by day/week
  - top disease detected
  - sync coverage / cloud-backed scans
- Avoid claiming predictive hotspot intelligence unless backend data exists

Priority 3: ann-yoddha-mobile UI/UX redesign
- Keep functionality intact but improve visual quality and navigation clarity
- Build a consistent visual system instead of isolated screen styling
- Make the app feel like one product, not separate screens
- Improve farmer-facing clarity and simplify the path from open app to scan result

Priority 4: final verification and deployment readiness
- Build web successfully
- Run mobile type-check or Expo-safe validation
- Run backend startup checks
- Document env vars, URLs, and deployment sequence
- Prepare final deployment checklist for later

What I need for the analysis section

Minimum product decisions required
- Decide whether analysis means historical farm insights or advanced predictive intelligence
- Decide whether version 1 should be:
  - real history-based analytics only
  - or history-based analytics now, predictive intelligence later

Recommended version 1 scope
- Use real backend scan history only
- Show:
  - total scans
  - healthy scans
  - disease-positive scans
  - most frequent disease
  - latest diagnosis
  - recent diagnosis trend
  - confidence distribution or average confidence
- Mark advanced predictive hotspot forecasting as future work unless actual model/data pipeline exists

Data needed if you want richer analysis
- Region or village per user or per scan
- Crop growth stage
- Scan date normalization
- Weather data source or external disease-risk signal
- Enough scan history volume to display trends honestly
- A rule or model for severity labeling
- A real source for hotspot aggregation across users/regions

Questions that must be answered in implementation
- Is analysis user-specific only, or cross-user/global?
- Should farmer see only personal scans, or village/region summary too?
- Is region stored in backend right now? If not, hotspot analytics cannot be truthful
- Should analytics use only saved history rows, or additional metadata tables?

Recommended implementation decision for today
- Ship a truthful Analysis page based on existing history data
- Defer hotspot forecasting and predictive disease spread until data pipeline exists
- Keep UI polished, but content must be real

Detailed mobile UI/UX redesign scope

Design direction
- Visual style: clean agricultural SaaS, not generic student prototype
- Tone: trustworthy, calm, field-ready, fast to understand
- Palette direction:
  - primary deep green
  - warm cream background
  - muted earth neutrals
  - restrained alert red/orange for disease states
- Use spacing, card hierarchy, and typography consistently across all screens
- Reduce visual noise and remove anything that feels temporary

Navigation improvements needed
- Bottom tabs should have cleaner labels, spacing, and active states
- Header should feel consistent and not crowded
- Logout action should not visually compete with primary actions
- Primary path should be obvious:
  - Dashboard
  - Scan Crop
  - Review History
- Consider making Scanner the center or strongest action if it matches product usage

Auth screens redesign tasks
- Replace plain form look with a strong branded auth layout
- Add clear product message:
  - diagnose wheat disease quickly
  - save history
  - sync when connected
- Improve input spacing, error messages, button states, and loading states
- Ensure login/register switch is obvious
- Make demo/test accounts easy to use if still needed

Dashboard redesign tasks
- Improve top summary card with cleaner hierarchy and better spacing
- Surface latest diagnosis as the main content block
- Add clearer quick actions:
  - Scan Now
  - Sync Pending Data
  - View History
- Show at-a-glance stats with better balance and less cramped cards
- Show pending sync count accurately, not only from latest scan
- Make health summary more readable and less decorative

Scanner redesign tasks
- Improve camera overlay
- Add clearer capture guidance before shot
- Improve analyzing state with professional loading text
- Add a post-capture result sheet or result card experience
- Reduce reliance on alert popups for the main diagnosis flow
- Prefer in-screen result presentation over basic modal alerts
- Make permission-denied state more helpful and more polished

History redesign tasks
- Improve empty state
- Improve card layout with image, diagnosis, confidence, time, sync badge
- Add filter capability if time permits:
  - all
  - healthy
  - disease detected
  - unsynced
- Ensure cards are easier to scan quickly

Shared UI system tasks for mobile
- Create shared design tokens if not already present:
  - colors
  - spacing
  - radius
  - typography
  - shadows
- Create reusable building blocks where useful:
  - screen container
  - section title
  - status badge
  - primary button
  - secondary button
  - summary card
- Keep refactor practical; do not over-abstract before shipping

Detailed execution order for today

Step 1: inspect and stabilize backend
- Run backend startup
- Fix missing dependencies or import/runtime issues
- Verify auth endpoints manually
- Verify predict endpoint with real image
- Verify sync endpoint with test payload

Step 2: lock the analysis page scope
- Convert web analytics from placeholder framing to real history-based analytics where possible
- Define what remains future work
- Remove claims that suggest unavailable forecasting

Step 3: redesign ann-yoddha-mobile UI/UX
- Start with shared styles/tokens
- Redesign navigation shell
- Redesign auth screens
- Redesign dashboard
- Redesign scanner flow
- Redesign history screen
- Ensure the whole app feels visually coherent

Step 4: verify end-to-end app flows
- Mobile login
- Mobile scan online
- Mobile offline save
- Mobile sync
- Web login
- Web diagnosis
- Web dashboard history
- Web analysis page

Step 5: prepare deployment handoff
- Finalize env vars
- Finalize backend URL strategy
- Identify mobile build/deployment path
- Identify web hosting target
- Identify backend hosting target
- Write exact deployment order

Concrete tasks still to do in code

Backend tasks
- Verify whether history endpoint is complete and consistent with web usage
- Improve API error responses for bad auth, invalid image, and model failures
- Confirm request/response contracts used by mobile and web are stable
- Confirm CORS includes actual web/mobile origins needed for deployment
- Add any missing health/readiness checks if useful

Web tasks
- Replace demo-only analytics dependency where possible with real summaries
- Ensure empty states are polished and honest
- Check diagnosis page result UX
- Check token persistence and route protection edge cases
- Confirm production build remains clean

Mobile tasks
- Add shared UI constants/theme file if missing
- Improve tab bar styling and header actions
- Reduce alert-driven UX in scanner and replace with in-screen status/result UI
- Improve diagnosis presentation language and layout
- Improve sync feedback and pending count accuracy
- Improve empty and loading states across screens

Specific analytics tasks for version 1
- Add backend-driven summary endpoint if current frontend would be cleaner with one
- Or compute from existing history response if faster and sufficient
- Show:
  - total scans
  - healthy vs diseased split
  - top detected diseases
  - recent diagnosis activity
- Defer:
  - disease hotspots by region
  - seasonal predictive forecasting
  - severity modeling based on external data

What to avoid today
- Do not invent predictive intelligence without data
- Do not build a second auth system
- Do not rebuild backend structure already completed
- Do not spend time on low-value abstraction
- Do not let mobile UI redesign break scan/auth/sync functionality
- Do not deploy before runtime verification is complete

Environment and deployment preparation

Backend deployment prep
- Confirm required packages in requirements.txt and/or pyproject.toml
- Confirm startup command
- Confirm env vars:
  - SECRET_KEY
  - ACCESS_TOKEN_EXPIRE_MINUTES
  - DATABASE_URL if switching from sqlite
  - allowed client origins if needed
- Decide host platform later:
  - Render
  - Railway
  - Fly.io
  - other

Web deployment prep
- Confirm env var for backend base URL
- Confirm production build command
- Confirm static hosting target later:
  - Vercel
  - Netlify
  - other

Mobile deployment prep
- Confirm EXPO_PUBLIC_BACKEND_URL
- Confirm production API URL handling
- Confirm camera/secure storage permissions are correct
- Decide whether deployment means Expo preview build first or full APK build later

Definition of done for today
- Backend starts without crashing
- Auth works end to end
- Predict works end to end
- Sync works end to end
- Web app builds and uses real backend flows
- Analysis page is real and truthful, not fake-placeholder product copy
- Mobile app looks professional, easier to navigate, and visually consistent
- Context and deployment steps are documented clearly enough for next action

Immediate next action for implementation
- Start with backend verification first because every client depends on it
- After backend is stable, implement analysis page truthfully
- Then redesign ann-yoddha-mobile UI/UX screen by screen
- Finish with verification and deployment checklist

Notes for the next work session
- Use this file as the source of truth for execution order
- If time gets tight, cut predictive analytics first, not core auth/scan/sync quality
- If a feature cannot be made real today, downgrade scope and present it honestly instead of shipping placeholders
